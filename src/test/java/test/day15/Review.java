package test.day15;

public class Review {
    //todo
    // *Allerts: simple alert just gives permission to press the "ok" button.
    // confirm: has two options which is "ok" and "cancel"
    // propt: used for getting info from the user and pressing the ok&cancel button.
    // accept()==> is used for "ok"
    // dismiss()==> is used for "cancel"
    // sendkeys()==> is used for the input of a string
    // getText()==> is used for getting text from WebElement
    // switchto()==> must be used.
    //
}
